# cp .env.example .env して値を埋める。.env はルートの .gitignore の *.env パターンに一致し、
# git管理対象外（実行ホスト上にのみ置く）。

# Claude Code / Claude Agent SDK 認証
ANTHROPIC_API_KEY=

# GitHub App認証（詳細は tools/sandbox/github_app/ 実装時に定義。ここではプレースホルダのみ）
GITHUB_APP_ID=
GITHUB_APP_PRIVATE_KEY_PATH=
GITHUB_APP_INSTALLATION_ID=
