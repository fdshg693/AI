// このファイルを対象リポジトリのルートに `.linear-cli/config.json` としてコピーして使う。
// 省略可能なファイル。存在しない場合は team/project の絞り込み無しで動作する
// （--team/--project コマンドライン引数の指定がこのファイルより優先される）。
//
// JSON5/JSONCではなく素のJSONとして読み込むため、コメント行はコピー後に削除すること。
{
  "team": "ENG",
  "project": "My Project"
}
