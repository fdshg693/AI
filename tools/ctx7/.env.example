# Context7 API key. Optional -- confirmed by hand that both REST endpoints
# this CLI calls (libs/search, context) answer HTTP 200 with NO key at all,
# just at a lower rate limit than an authenticated request. Leave this blank
# (or delete the line) to run unauthenticated.
CONTEXT7_API_KEY=
