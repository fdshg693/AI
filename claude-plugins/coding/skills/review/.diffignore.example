# diff_report.py がレビュー対象から除外するパスパターンを記載するファイル。
# 書式は .gitignore と同じ（例: 末尾 `/` はディレクトリ、`*` はワイルドカード、`#` はコメント）。
# パスは git のリポジトリルートからの相対パスにマッチさせる。
#
# 例:
# *.lock
# *.min.js
# dist/
# package-lock.json
