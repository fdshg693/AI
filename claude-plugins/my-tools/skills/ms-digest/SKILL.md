---
name: ms-digest
description: Microsoft Learn/Azure公式ドキュメントを`mslearn`で検索した結果ファイルのうち、自分で1件ずつ読む代わりに`aim-ask`で並列にAI抽出させたいものをまとめて処理し、関連する内容だけを報告させるCLI連携スキル。(a) 結果件数が多くタイトルだけでは関連性を判断しづらい場合(目安5件以上)に全件を渡して関連するものだけ絞り込ませたいとき、(b) `index.md`から読みたいファイルを選んだ後、その中に文字数が大きいファイルが複数あり個別Readせずまとめて抽出させたいとき、のいずれかで使う。1ファイルだけを抽出したい場合は`aim-ask`を直接使えばよい。

# 前提条件(このスキル自体はインストール・セットアップを一切行わない):
#   - `mslearn` コマンドが PATH 上で使えること
#     (セットアップは tools/mslearn/README.md 参照)
#   - `aim-ask` コマンドが PATH 上で使え、OPENROUTER_API_KEY が設定済みであること
#     (セットアップは claude-plugins/my-tools/skills/aim-ask/SKILL.md 参照)
#
# 依存スキル: claude-plugins/my-tools/skills/ms-learn (検索/フェッチ本体)
#            claude-plugins/my-tools/skills/aim-ask (並列AI抽出本体)
# このスキルは上記2つを繋ぐ薄いオーケストレーション層で、自前のソースコードは持たない。
meta:
  tag: []
  requires_repo_tools: none
  requires_env: OPENROUTER_API_KEY
  dependencies: mslearn, aim-ask
  requires_install: none
  requires_hooks: none
  requires_skills: ms-learn, aim-ask
  status: stable
  description: no description
  version: 1.2.0
---

## 全体の流れ

```markdown
1. mslearn search "<query>" / code-search "<query>" [--language <lang>] を実行する
   → 結果フォルダの index.md パスだけが返る(個別ファイルのパス・本文はここでは
   分からない)

2. index.md を Read し、結果ファイルへの相対リンク一覧(タイトル・各件の文字数付き)を
   把握する(フォルダ直下 = 公式ドキュメント、qa/ 配下 = Microsoft Q&A)。ここで
   aim-ask に渡す対象を決める:
   (a) タイトルだけでは関連性が判断しづらい・件数が多い(目安5件以上) → 全件を渡す
   (b) すでに読みたいファイルはタイトルから絞れている → その中で文字数が大きい
   ものだけを渡す(小さいものはそのまま個別に Read すればよく、このスキルは
   使わない)

3. 元の質問/知りたいことを1本の「抽出プロンプト」に言語化する(下記「抽出プロンプトの
   作り方」参照)。aim-ask は全ファイルに同一プロンプトを使うため、ファイル名や個々の
   タイトルはプロンプトに埋め込まない

4. 本実行の前に、手順2で選んだ結果ファイルのうち1件だけを`--jobs 1`でaim-askに投げ、
   エラーなく応答が返ることを確認する(モデル側の問題を全件投入前に切り分けるため。
   下記「注意点」参照)

5. 手順2で選んだ結果ファイルパスを"まとめて1回"aim-askに渡す
   aim-ask <folder>/0001.md <folder>/0002.md ... [<folder>/qa/0003.md ...] \
   --prompt "<抽出プロンプト>" --jobs <並列数>
   → 1コマンドで選んだ結果への並列AI呼び出しが走り、ファイルごとの応答が返る。
   Claude自身が個別ファイルをReadする必要はない

6. aim-askの応答を見て、「関連なし」以外の項目だけをユーザーへの回答に統合する。
   抽出プロンプトに「URL:/Link:行があれば転記して」と指示しておけば、出典URLも
   追加のReadなしで拾える(各結果ファイルの冒頭にmslearn側が埋め込んでいる)

7. 抜粋だけでは情報不足な場合だけ、該当ページを mslearn fetch <URL> でフルページ取得し、
   必要なら同じ要領でaim-ask(1ファイルのみでも可)に該当箇所を抽出させる
```

手順1・2・7は `ms-learn` スキル(`claude-plugins/my-tools/skills/ms-learn/SKILL.md`)と
完全に同じ挙動。オプション・出力フォルダ仕様・終了コードなどの詳細はそちらを参照し、
ここでは重複させない。

## 抽出プロンプトの作り方

`aim-ask` は全ファイルに**同一の**`--prompt`を使う(ファイルごとに変えられない)。
「何を探しているか」をファイル非依存の形で言語化するのがコツ。

例: ユーザーの質問が「Azure Functions の Python v2 モデルでタイムアウトを変更する方法」の場合

```text
以下はMicrosoft公式ドキュメントの検索結果の抜粋です。
「Azure Functions の Python v2 プログラミングモデルでタイムアウト時間を変更する方法」
に関連する記述があれば、該当箇所を日本語で簡潔に抜き出してください。
冒頭付近の `URL:` または `Link:` 行があれば、それも回答の末尾にそのまま転記してください。
関連する記述が無ければ、他には何も書かず「関連なし」とだけ答えてください。
```

- ❌ 単に「要約してください」だけだと、無関係な結果まで律儀に要約されてしまいフィルタ
  リングにならない。**「関連が無ければ関連なしとだけ答える」を必ず明示する**
- 抜粋が「詳しくは...を参照」のように途中で切れているなど、情報が不十分そうな兆候を
  拾いたい場合は、抽出プロンプトに「情報が不十分そうなら『要フルページ取得』と付記して」
  も足しておくと、手順7に進むべきファイルをaim-askの応答だけから判断できる
- `code-search` の結果に対しては「探しているコードパターン/実装したい処理」を軸に
  同様のプロンプトを組み立てる
- 抽出プロンプトにバッククォートや引用符、長い日本語文が混じるとシェルの
  `--prompt "..."` インライン指定でエスケープを誤りやすい。同一プロンプトを
  複数クエリ/複数回のaim-ask呼び出しで使い回す場合は、`.aim-use/aim-ask.toml`の
  `prompt`キーに一度書いておき`--prompt`を省略する方が安全(`aim-ask`スキル参照)

## 並列数(`--jobs`)の目安

- `search`結果は既定で最大10件前後(docs+qaを合わせるとさらに増える)。`aim-ask`の既定
  `--jobs 4`でも十分速いが、件数が多い/急ぎなら`--jobs 8`〜`10`(aim-askの上限)まで
  上げてよい
- 複数クエリの結果フォルダをまたいで一括処理したい場合も、全ファイルパスを1回の
  `aim-ask`呼び出しにまとめて渡せば同様に並列化される
- `--jobs`を上げても速くなるのはAI呼び出しが正常に進む場合のみ。既定モデルが
  利用不可などモデル/API側に問題がある場合は全ファイルが同じエラーで即失敗するだけ
  なので、まず「注意点」の1件スモークテストで切り分ける

## 使い分け: このスキル vs `ms-learn` スキル

| 状況                                                                                        | 使うスキル                                |
| ------------------------------------------------------------------------------------------- | ----------------------------------------- |
| 結果が1〜2件で、内容もすぐ読める規模                                                        | `ms-learn`(直接Readした方が単純)          |
| index.md でタイトルから選んだファイルが小さい(文字数が少ない)                               | `ms-learn`(そのまま個別に Read)           |
| 件数が多くタイトルだけでは関連性を判断しづらい(目安5件以上)、複数クエリ分をまとめて捌きたい | `ms-digest`(このスキル・全件投入)         |
| index.md で選んだファイルの中に文字数が大きいものが複数ある                                 | `ms-digest`(このスキル・選んだ分だけ投入) |
| すでに対象URLが分かっていて全文フェッチだけしたい                                           | `ms-learn`の`fetch`をそのまま使う         |

いずれの場合も判断は必ず `index.md` を Read した後に行う(mslearn 実行直後に返るのは
`index.md` のパスのみで、集計値は返らない)。「大きい」の閾値は会話の残りコンテキスト量
や個別ファイルの内容次第で変わるため固定値はなく、index.md に付記された各ファイルの
文字数を見て都度判断する。

## 注意点

- `aim-ask`はステートレスでDB永続化はしない。同じ検索をやり直す場合は都度呼び直す
  ことになる(mslearn側の結果ファイル自体は使い捨てフォルダに残るため再利用は可能)
- 個別ファイルの読み込み失敗・AI呼び出し失敗は、そのファイルだけ失敗として記録され
  他ファイルの処理は継続する(`aim-ask`スキル参照)。失敗したファイルは`mslearn fetch`
  など代替手段で個別に読む
- `mslearn`/`aim-ask`双方のインストール手順・環境変数・終了コードなどのセットアップ/
  仕様はこのスキルでは扱わない。未設定エラーが出た場合はそれぞれのスキルの案内に従う
- **全ファイルが同一のエラーメッセージで失敗した場合は、個別ファイルの問題ではなく
  モデル/API側の問題を疑う**(例: 既定の無料枠モデルが提供停止でOpenRouter呼び出しが
  全滅する、など)。この場合は手順7の個別フォールバック(`mslearn fetch`)に進んでも
  無意味なので、`--model`に別のモデル略記(`aim --list-models`参照)を明示して
  全件を再実行する
