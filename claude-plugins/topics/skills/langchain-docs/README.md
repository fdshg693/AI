# langchain-docs

## 補足

- LangChain 公式は `https://reference.langchain.com/mcp` という MCP サーバー(`search_api`/`get_symbol` ツール)も提供している。IDE統合向けの選択肢として存在は把握しておくが、このスキルはローカルの `llms.txt`/`llms-full.txt` + WebFetch で完結させる方針とし、MCPサーバーの追加設定は行わない
