The list below contains entries from the VS Code official documentation index
(llms.txt) that are NOT present in `output/copilot-excerpt.md` (the curated
GitHub Copilot / AI agent excerpt), and also did NOT match the keyword heuristic
(i.e. they were mechanically judged as unrelated). Entries are grouped under
`## Section name` headings, in the form `- [Title](URL): description`.

Check whether any entry in this list is actually about GitHub Copilot / AI agent
functionality in VS Code (Chat, Agent mode, MCP, custom instructions, etc.) despite
being missed by the keyword heuristic (a false negative). Because this list is
large, you do not need to write a reason for every entry that is obviously
unrelated (plain Git operations, terminal settings, remote development, etc.).
Only list entries you suspect are false negatives, with their title, URL, and a
brief reason. If you find none, just say so explicitly.
